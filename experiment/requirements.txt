# Analysis dependencies — enough to run analysis.ipynb end to end.
# Versions are the ones the reported results were produced with.
numpy==2.5.2
pandas==3.0.5
matplotlib==3.11.1
pillow==12.3.0
jupyterlab==4.6.3
nbformat==5.11.1

# Collection only — needed by server.py and run_experiment.py, not by the
# analysis. Omit these if you are just re-running the notebook.
flask==3.1.3
selenium==4.47.0
requests==2.34.2
